import monday from '$lib/api/monday.server';

/** @type {import('./$types').RequestHandler} */
export async function GET() {
	// const res = await monday.createItem();
	// console.log(res);
	return new Response();
}
